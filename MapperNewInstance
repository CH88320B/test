


.Map(dest => dest.Insurances, src =>
{
    var personInsurance = src.Person != null ? src.Person.Insurance : null;
    return personInsurance != null && personInsurance.Any()
        ? personInsurance.Select(insurance => new Insurance
        {
            // ... lógica ...
        }).ToList()
        : new List<Insurance>();
})
